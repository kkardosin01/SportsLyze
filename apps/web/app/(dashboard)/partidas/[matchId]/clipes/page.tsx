export default function ClipesPage() {
  return (
    <div className="space-y-4">
      <h2 className="font-semibold">Clipes por evento</h2>
      <p className="text-sm text-gray-500">
        A detecção automática de eventos (passes, dribles, faltas, finalizações, triangulações) e
        o corte de clipes fazem parte da Fase 2 do roadmap. Esta tela já está preparada na
        arquitetura — schema de <code>events</code> e <code>clips</code> disponível desde a Fase 1.
      </p>
    </div>
  );
}
